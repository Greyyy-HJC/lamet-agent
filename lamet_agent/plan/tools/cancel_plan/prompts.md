Cancel the planning conversation without accepting the candidate or entering Run
mode. Use this only for an explicit natural-language request to quit or cancel.

Call schema: this tool takes no arguments; call it with `{}`. The observation
sets `cancelled=true`, which terminates the Plan conversation.
