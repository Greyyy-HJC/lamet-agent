Save the current candidate without accepting the Plan or entering Run mode. Use
this for a natural-language request to save progress; validator Issues may remain.

Call schema: this tool takes no arguments; call it with `{}`. The observation
returns the saved path and current number of unresolved Issues.
