Inspect all contract rules below an existing or proposed manifest path, including
valid areas for which the validator reports no Issue. Use this before implementing
an explicit request to add or change a valid stage, job, systematic variation,
strategy, or parameter set.

Call schema: `path` is one required, nonempty dotted manifest path. The observation
returns the current value, contract anchor, allowed children, and related rules.
