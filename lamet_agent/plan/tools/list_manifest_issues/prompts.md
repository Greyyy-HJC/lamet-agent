Read every current validator Issue without merging jobs or assuming that repeated
gaps share one value. Each packet includes the current value, parent context,
allowed siblings, and all related contract child rules.

Call schema: this tool takes no arguments; call it with `{}`. Translate the
returned contract and physics evidence into understandable scientific questions
instead of presenting unexplained keys or bare option tokens to the user.
