Write only after deterministic inspection, consistency checks, literature ranking,
and paper reading. Author every narrative section, including scope and provenance,
data and parameter coverage, and a required interpretation of the supplied
consistency findings. Numerical claims must come from run evidence, and citations
may name only papers selected by read_papers. The framework will not add scope,
coverage, or consistency prose after the call, so make those sections complete.
It will append deterministic scope counts and a coverage table, but it will not
append or rewrite the consistency interpretation.
