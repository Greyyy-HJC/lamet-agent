Read the full-resolution center and uncertainty for exactly one job already selected
by this Review. Use this only when the default 60-point evidence cannot support a
material physical statement. Pass one exact `job_id`; do not retrieve jobs merely
for completeness or repeat a completed read.
