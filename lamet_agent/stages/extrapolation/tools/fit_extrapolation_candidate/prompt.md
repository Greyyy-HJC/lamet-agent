Select a complete term set from the authored required and allowed terms. Pass an
empty exclusion list because the reference extrapolation uses every authored input.
The candidate remains in state until comparison.
