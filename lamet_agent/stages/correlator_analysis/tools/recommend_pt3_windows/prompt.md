Select a nonempty ordered list of three-point windows. Each window must contain
a nonempty unique subset of the available `tsep` values and one nonnegative
`tau_cut`. Exclude source and sink contamination while retaining a visible
central signal with useful precision. Use distinct `tau_cut` values for
alternative windows. An FH scope needs at least two selected `tsep` values.
