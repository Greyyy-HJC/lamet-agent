Publish only after inspection and every authored candidate has been tuned.
Select the candidate recommended by the original data-window rule: require an
overdetermined fit, prefer Q-passing candidates, keep fits within 0.25 of the
best chi2/dof, and among those preserve the largest data window. For a
spectral-model candidate, this tool preflights all z centers, removes and
reselects any numerically unusable candidate internally, then performs the one
full-z, all-sample fit, writes the canonical sample-bearing output, and finishes.
