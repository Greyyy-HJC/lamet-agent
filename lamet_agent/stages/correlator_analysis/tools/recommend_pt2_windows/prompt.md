Select a nonempty ordered list of two-point fit windows. Use later-time regions
with stable central behavior while retaining useful precision. Every window must
lie inside `allowed_time_range`, have `tmin < tmax`, and contain at least
`minimum_points` available time coordinates. Include a small set of defensible
alternatives when the onset of stability is ambiguous.
