Inspect the declared descriptor and choose a subset only when the stage job needs
one. The observation reports dimensions, sample count, coordinate ranges, and the
shared resample id; raw arrays remain in context state. For spectral fits the
tool also determines a power-of-ten correlator scale from the authored two-point
windows, placing the typical scaled magnitude in the original target range.
